import { styled } from '@mui/material/styles';
import Button from '@mui/material/Button';

export const BullButton = styled(Button)({
	borderRadius: "8px",
	textTransform: "initial",

	boxShadow: 'none',
});